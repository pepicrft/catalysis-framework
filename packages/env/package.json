{
  "name": "@gestaltjs/env",
  "version": "0.3.2",
  "private": true,
  "description": "A set of utilities for getting the environment information",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "/src",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "scripts": {
    "clean": "shx rm -rf dist",
    "prepack": "pnpm run build",
    "build": "true",
    "lint": "prettier -c src/** && eslint src/**/*.ts",
    "lint:fix": "prettier -w src/** && eslint src/**/*.ts --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "tsc": "tsc --noEmit"
  },
  "eslintConfig": {
    "extends": [
      "../../package.json"
    ]
  },
  "devDependencies": {
    "@gestaltjs/core": "0.3.2",
    "@gestaltjs/testing": "0.1.0",
    "@oclif/core": "1.3.4",
    "@types/envinfo": "^7.8.1",
    "envinfo": "^7.8.1",
    "pino-pretty": "^7.5.1"
  },
  "oclif": {
    "commands": "./dist/commands",
    "plugins": []
  }
}
