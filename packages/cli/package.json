{
  "name": "@gestaltjs/gestalt",
  "version": "0.5.0",
  "description": "A CLI tool for building Gestalt web app",
  "license": "MIT",
  "private": false,
  "bin": {
    "gestalt": "./bin/gestalt.js"
  },
  "dependencies": {
    "yargs": "^17.3.1"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "version:minor": "pnpm version minor",
    "postversion": "git add package.json && git push && git push --tags && pnpm run release",
    "release": "pnpm publish"
  }
}
