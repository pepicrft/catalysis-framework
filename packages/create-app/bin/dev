#!/usr/bin/env bash
set -e

DIRECTORY=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

echo "Building..."
pnpm build --dir $DIRECTORY/.. &> /dev/null

$DIRECTORY/create-app.js $@
