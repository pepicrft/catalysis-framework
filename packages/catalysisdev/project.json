{
  "name": "catalysisdev",
  "root": "packages/catalysisdev",
  "sourceRoot": "packages/catalysisdev/src",
  "projectType": "library",
  "implicitDependencies": [
    "build",
    "check",
    "db",
    "dev",
    "generate",
    "info",
    "routes",
    "test",
    "eslint-plugin"
  ],
  "tags": ["scope:cli"],
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm shx rm -rf dist",
        "cwd": "packages/catalysisdev"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["dist/"],
      "inputs": ["{projectRoot}/src/**/*"],
      "options": {
        "command": "pnpm tsc -b ./tsconfig.build.json",
        "cwd": "packages/catalysisdev"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint './**/*.{js,ts}'",
        "cwd": "packages/catalysisdev"
      }
    },
    "lint:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint './**/*.{js,ts}' --fix",
        "cwd": "packages/catalysisdev"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm vitest run",
        "cwd": "packages/catalysisdev"
      }
    },
    "test:watch": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "pnpm vitest watch",
        "cwd": "packages/catalysisdev"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm tsc --noEmit",
        "cwd": "packages/catalysisdev"
      }
    }
  }
}
