{
  "name": "@gestaltjs/core",
  "version": "0.5.0",
  "private": false,
  "description": "Set of Getalt tools and models that are shared across all the features",
  "keywords": [
    "gestalt",
    "gestaltjs",
    "web"
  ],
  "license": "MIT",
  "type": "module",
  "exports": {
    "./cli/logger/transport": {
      "import": "./dist/cli/logger/transport.js",
      "types": "./dist/cli/logger/transport.d.ts"
    },
    "./cli": {
      "import": "./dist/cli/index.js",
      "types": "./dist/cli/index.d.ts"
    },
    "./framework": {
      "import": "./dist/framework/index.js",
      "types": "./dist/framework/index.d.ts"
    }
  },
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean": "shx rm -rf dist",
    "prepack": "cross-env NODE_ENV=production pnpm run build",
    "build": "pnpm clean && rollup -c",
    "lint": "prettier -c src/** && eslint src/**/*.ts",
    "lint:fix": "prettier -w src/** && eslint src/**/*.ts --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --reporter=default --reporter=junit --outputFile=./junit-core.xml",
    "tsc": "tsc --noEmit"
  },
  "eslintConfig": {
    "extends": [
      "../../package.json"
    ]
  },
  "dependencies": {
    "zod": "^3.11.6"
  },
  "devDependencies": {
    "@iarna/toml": "^2.2.5",
    "@types/pino": "^7.0.5",
    "@types/shelljs": "^0.8.11",
    "case-anything": "^2.1.10",
    "fastify": "^3.27.2",
    "find-up": "^6.2.0",
    "fs-extra": "^10.0.0",
    "noop-stream": "^1.0.0",
    "pathe": "^0.2.0",
    "picocolors": "^1.0.0",
    "pino-abstract-transport": "^0.5.0",
    "shelljs": "^0.8.5",
    "tempy": "^2.0.0",
    "terminal-link": "^3.0.0",
    "vite": "^2.8.4",
    "zod": "^3.11.6"
  },
  "engine-strict": true,
  "engines": {
    "node": "^14.13.1 || >=16.0.0 || >=17.0.0"
  },
  "os": [
    "darwin",
    "linux",
    "win32"
  ]
}
