/**
 * This type declares the interface of a database target defined by the user.
 * Note there's also an internal counterpart type, DatabaseTarget, that augments
 * this type with additional properties and methods that are internal to the framework.
 * For example the directory containing the target.
 */
export type Database = {
  // Nothing yet
}
